schema_version: 1

context:
  name: prek
  version: "0.3.3"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://github.com/j178/${{ name }}/archive/refs/tags/v${{ version }}.tar.gz
  sha256: 22cc379f34c6d9e363d65f6082df763051eab87676c63b974916d1f06f55d254

build:
  number: 0
  script:
    env:
      CARGO_PROFILE_RELEASE_STRIP: symbols
      CARGO_PROFILE_RELEASE_LTO: fat
    content:
      - cargo auditable install --locked --no-track --bins --root ${{ PREFIX }} --path crates/prek
      - cargo-bundle-licenses --format yaml --output ./THIRDPARTY.yml

requirements:
  build:
    - ${{ stdlib('c') }}
    - ${{ compiler('c') }}
    - ${{ compiler('rust') }}
    - cargo-bundle-licenses
    - cargo-auditable

tests:
  - script:
      - prek --help

about:
  homepage: https://prek.j178.dev
  summary: Better `pre-commit`, re-engineered in Rust
  repository: https://github.com/j178/prek
  license: MIT
  license_file:
    - LICENSE
    - THIRDPARTY.yml
    - licenses/LICENSE.pre-commit.txt
    - licenses/LICENSE.identify.txt
    - crates/prek-pty/LICENSE

extra:
  recipe-maintainers:
    - weiji14
    - ocefpaf
